const { Module } = require("module")


var url = {

    bc: {
        host: "127.0.0.1",
        port: "3000",
    },
    pblocal: {
        host: '127.0.0.1',
        port: '3000'
    }
}

module.exports = url;
